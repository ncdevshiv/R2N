[package]
name = "r2n-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "R2N interlinked IR (JS/React/Runtime) and lowering passes"

[dependencies]
r2n-ast = { path = "../r2n-ast" , version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
r2n-compiler = { path = "../r2n-compiler" , version = "0.1.0" }
